spring:
  application:
    name: LikeLionCatchit

  datasource:
    url: jdbc:mysql://localhost:3306/likelion
    username: root
    password: 'na76601125**'

  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
